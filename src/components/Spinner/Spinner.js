import React from "react";
import { SpinnerContainer, SpinnerOverlay } from "./Wrapper";
const Spinner = () => {
	return (
		<SpinnerOverlay>
			<SpinnerContainer />
		</SpinnerOverlay>
	);
};

export default Spinner;
